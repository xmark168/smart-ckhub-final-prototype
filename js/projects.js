(function(){
  var screen=document.getElementById('projects');if(!screen||!window.CKHubCustomerData)return;
  var oldDetail=document.getElementById('projectDetail');if(oldDetail)oldDetail.remove();
  var base=window.CKHubCustomerData.concat([['Ẩm Thực Phước Quắn','Hải','HCM']]);
  var owners=['Tuyền','Nguyên','Hiền','Minh Anh','Hải'];
  var state={query:'',status:'',owner:'',area:'',risk:false,kpi:'active',page:1,pageSize:20,selected:null};
  var records=base.map(function(row,index){var stateName=index===20?'stopped':index%13===0?'draft':index%11===0?'pending':'active',risk=stateName==='active'&&index%6===0,cycle=(index%6)+1,total=index%4===0?3:6,progress=Math.min(92,24+(index*9)%69),due=['23.09.2026','29.09.2026','30.09.2026','01.10.2026'][index%4];return {id:'project-'+(index+1),code:'DA-2026-'+String(index+1).padStart(3,'0'),customer:row[0],owner:owners[index%owners.length],createdBy:owners[(index+2)%owners.length],area:row[2],service:index%3===0?'Social Content':index%3===1?'Content duy trì':'Ads duy trì',state:stateName,risk:risk,cycle:cycle,total:total,progress:progress,due:due,posts:index%3===0?8:12,shooting:index%4===0?2:1,tasks:risk?3:(index%4)+1}});
  function esc(value){return String(value).replace(/[&<>"']/g,function(char){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]})}
  function navigate(id){if(typeof window.showScreen==='function')window.showScreen(id);else document.querySelectorAll('.screen').forEach(function(item){item.classList.toggle('active',item.id===id)})}
  function label(item){return item.state==='active'?'Đang triển khai':item.state==='pending'?'Tạm dừng':item.state==='stopped'?'Đã dừng':'Dự án nháp'}
  function kind(item){return item.risk?'danger':item.state==='active'?'ok':item.state==='draft'?'muted':'waiting'}
  function filtered(){return records.filter(function(item){var text=[item.code,item.customer,item.owner,item.area,item.service].join(' ').toLocaleLowerCase('vi');return text.includes(state.query)&&(!state.status||item.state===state.status)&&(!state.owner||item.owner===state.owner)&&(!state.area||item.area===state.area)&&(!state.risk||item.risk)&&(state.kpi==='active'?item.state==='active':state.kpi==='risk'?item.risk:state.kpi==='paused'?item.state==='pending'||item.state==='stopped':true)})}
  function render(){var active=records.filter(function(item){return item.state==='active'}),risk=active.filter(function(item){return item.risk}),paused=records.filter(function(item){return item.state==='pending'||item.state==='stopped'}),list=filtered(),pages=Math.max(1,Math.ceil(list.length/state.pageSize));if(state.page>pages)state.page=pages;screen.innerHTML='<div class="projects-page"><div class="project-page-head"><h1>Dự án <button class="customer-help" id="projectHelp" aria-label="Quy tắc dự án">?</button></h1><button class="primary" id="createProject"><i data-lucide="plus"></i> Tạo dự án</button></div><section class="project-dashboard"><button class="project-kpi hero '+(state.kpi==='active'?'selected':'')+'" data-kpi="active"><label>Đang triển khai</label><strong>'+active.length+'</strong><small>'+risk.length+' dự án cần theo dõi</small></button><button class="project-kpi risk '+(state.kpi==='risk'?'selected':'')+'" data-kpi="risk"><label>Có rủi ro</label><strong>'+risk.length+'</strong><small>Trễ hoặc có nguy cơ trễ chu kỳ</small></button><button class="project-kpi '+(state.kpi==='paused'?'selected':'')+'" data-kpi="paused"><label>Tạm dừng / đã dừng</label><strong>'+paused.length+'</strong><small>Không tự đổi tiến độ hợp đồng</small></button></section><section class="project-list-shell"><div class="project-toolbar-new"><label class="project-search-new"><i data-lucide="search"></i><input id="projectSearchNew" type="search" value="'+esc(state.query)+'" placeholder="Tìm mã dự án, thương hiệu, Account…"></label><div class="project-filter-control"><button class="project-filter-trigger" id="projectFilterToggle" title="Lọc dự án"><i data-lucide="list-filter"></i>'+([state.status,state.owner,state.area,state.risk].filter(Boolean).length?'<span>'+[state.status,state.owner,state.area,state.risk].filter(Boolean).length+'</span>':'')+'</button><div class="project-filter-popover" id="projectFilterPopover"><div class="project-filter-popover-head"><b>Lọc dự án</b><button id="projectResetFilters">Xóa lọc</button></div><label>Trạng thái<select id="projectStatus"><option value="">Tất cả</option><option value="active">Đang triển khai</option><option value="pending">Tạm dừng</option><option value="stopped">Đã dừng</option><option value="draft">Dự án nháp</option></select></label><label>Account<select id="projectOwner"><option value="">Tất cả Account</option>'+owners.map(function(owner){return '<option value="'+owner+'">'+owner+'</option>'}).join('')+'</select></label><label>Khu vực<select id="projectArea"><option value="">Tất cả khu vực</option>'+Array.from(new Set(records.map(function(item){return item.area}))).map(function(area){return '<option value="'+area+'">'+area+'</option>'}).join('')+'</select></label><label class="filter-check"><input id="projectRisk" type="checkbox"> Chỉ dự án có rủi ro</label></div></div></div><div class="project-table-wrap"><table class="project-table-new"><thead><tr><th>Dự án</th><th>Account</th><th>Chu kỳ</th><th>Hạn chu kỳ</th><th>Tiến độ</th><th>Trạng thái</th><th></th></tr></thead><tbody>'+list.slice((state.page-1)*state.pageSize,state.page*state.pageSize).map(function(item){return '<tr data-id="'+item.id+'"><td><span class="project-record-name">'+esc(item.customer)+'</span><span class="project-record-meta">'+item.code+' · '+esc(item.service)+'</span></td><td>'+esc(item.owner)+'</td><td>'+item.cycle+' / '+item.total+'</td><td>'+item.due+'</td><td><div class="project-progress"><i style="--progress:'+item.progress+'%"></i><span>'+item.progress+'%</span></div></td><td><span class="pill '+kind(item)+'">'+label(item)+'</span></td><td><button class="project-open" aria-label="Mở '+esc(item.customer)+'">›</button></td></tr>'}).join('')+(list.length?'':'<tr><td colspan="7">Không tìm thấy dự án phù hợp.</td></tr>')+'</tbody></table></div><footer class="project-footer-new"><span>Hiển thị <b>'+ (list.length?((state.page-1)*state.pageSize+1)+'–'+Math.min(state.page*state.pageSize,list.length):0)+'</b> trong <b>'+list.length+'</b> dự án</span><div class="project-pager"><button id="projectPrev" '+(state.page===1?'disabled':'')+'>‹</button><button disabled>'+state.page+' / '+pages+'</button><button id="projectNext" '+(state.page===pages?'disabled':'')+'>›</button></div></footer></section></div>';bind();icons()}
  function icons(){if(window.lucide)window.lucide.createIcons({attrs:{'stroke-width':1.8}})}
  function bind(){var filter=screen.querySelector('.project-filter-control');screen.querySelector('#projectStatus').value=state.status;screen.querySelector('#projectOwner').value=state.owner;screen.querySelector('#projectArea').value=state.area;screen.querySelector('#projectRisk').checked=state.risk;screen.querySelector('#projectSearchNew').addEventListener('input',function(event){state.query=event.target.value.toLocaleLowerCase('vi');state.page=1;render();var input=screen.querySelector('#projectSearchNew');input.focus();input.setSelectionRange(input.value.length,input.value.length)});screen.querySelector('#projectFilterToggle').addEventListener('click',function(){filter.classList.toggle('open')});['projectStatus','projectOwner','projectArea','projectRisk'].forEach(function(id){screen.querySelector('#'+id).addEventListener('change',function(){state.status=screen.querySelector('#projectStatus').value;state.owner=screen.querySelector('#projectOwner').value;state.area=screen.querySelector('#projectArea').value;state.risk=screen.querySelector('#projectRisk').checked;state.page=1;render()})});screen.querySelector('#projectResetFilters').addEventListener('click',function(){state.status='';state.owner='';state.area='';state.risk=false;state.page=1;render()});screen.querySelectorAll('[data-kpi]').forEach(function(card){card.addEventListener('click',function(){state.kpi=card.dataset.kpi;state.page=1;render()})});screen.querySelector('#projectPrev').addEventListener('click',function(){state.page--;render()});screen.querySelector('#projectNext').addEventListener('click',function(){state.page++;render()});screen.querySelectorAll('tr[data-id]').forEach(function(row){row.addEventListener('click',function(){state.selected=records.find(function(item){return item.id===row.dataset.id});renderDetail()})});screen.querySelector('#projectHelp').addEventListener('click',function(){showInfo('Quy tắc dự án','Mỗi dự án thuộc một thương hiệu và một Account phụ trách. Chu kỳ luôn tính theo tháng. Dự án tạm dừng hoặc dừng không tự thay đổi số chu kỳ đã triển khai. Task thiếu Owner hoặc deadline không được bắt đầu.')});screen.querySelector('#createProject').addEventListener('click',function(){showInfo('Tạo dự án','Bản prototype sẽ mở luồng tạo dự án sau khi chốt trường Hợp đồng, gói dịch vụ và chu kỳ đầu tiên.')})}
  function renderDetail(){var item=state.selected;if(!item)return;var old=document.getElementById('projectWorkspaceDetail');if(old)old.remove();var detail=document.createElement('section');detail.id='projectWorkspaceDetail';detail.className='screen';detail.innerHTML='<div class="project-detail-head"><button class="project-detail-back" id="backProjects"><i data-lucide="arrow-left"></i> Dự án</button><div class="project-detail-title"><div><h1>'+esc(item.customer)+' <span class="pill '+kind(item)+'">'+label(item)+'</span></h1><p>'+item.code+' · '+esc(item.service)+' · Account '+esc(item.owner)+'</p></div><button class="secondary" id="editProjectDetail"><i data-lucide="pencil"></i> Sửa dự án</button></div></div><section class="project-overview"><div class="project-overview-main"><span>Sức khỏe triển khai</span><strong>'+ (item.risk?'Cần theo dõi':'Đúng tiến độ')+'</strong><p>'+ (item.risk?'Có '+item.tasks+' công việc cần Account rà soát trước hạn chu kỳ.':'Không có rủi ro đang mở trong chu kỳ này.')+'</p></div><div class="project-overview-stat"><span>Tiến độ hợp đồng</span><strong>'+item.cycle+' / '+item.total+'</strong><small>chu kỳ đã triển khai</small></div><div class="project-overview-stat"><span>Hạn chu kỳ '+item.cycle+'</span><strong>'+item.due+'</strong><small>mốc theo dõi</small></div></section><div class="project-detail-grid"><main><section class="panel"><div class="panel-head"><div><h2>Đầu ra chu kỳ</h2><p class="subline">Theo dõi phạm vi đã cam kết trong tháng.</p></div><span class="pill '+kind(item)+'">'+label(item)+'</span></div><div class="project-delivery-grid"><div><span>Bài đăng</span><b>'+item.posts+'</b><small>kế hoạch trong kỳ</small></div><div><span>Shooting</span><b>'+String(item.shooting).padStart(2,'0')+'</b><small>lịch trong kỳ</small></div><div><span>Công việc mở</span><b>'+String(item.tasks).padStart(2,'0')+'</b><small>'+ (item.risk?'cần xử lý':'đang theo dõi')+'</small></div></div></section><section class="panel"><div class="panel-head"><h2>Tiến độ gần đây</h2></div><div class="project-status-row"><i data-lucide="list-checks"></i><span><b>Account đã rà soát tiến độ chu kỳ</b><small>Hôm nay · '+item.code+'</small></span></div><div class="project-status-row"><i data-lucide="file-check-2"></i><span><b>Content Plan đã cập nhật</b><small>Hôm qua · Chu kỳ '+item.cycle+'</small></span></div><div class="project-status-row"><i data-lucide="calendar-clock"></i><span><b>Lịch shooting đang được theo dõi</b><small>Tuần này · '+item.shooting+' lịch trong kỳ</small></span></div></section></main><aside><section class="panel"><div class="panel-head"><h2>Account phụ trách</h2></div><button class="customer-account-card"><i>'+esc(item.owner).slice(0,2).toUpperCase()+'</i><span><b>'+esc(item.owner)+'</b><small>Điều phối timeline và nguồn lực</small></span><em data-lucide="chevron-right"></em></button></section><section class="panel"><div class="panel-head"><h2>Kiểm soát dự án</h2></div><button class="customer-control '+(item.risk?'is-attention':'')+'" id="toggleRisk"><i data-lucide="flag"></i><span><b>'+ (item.risk?'Đang gắn cờ cần chú ý':'Đánh dấu cần chú ý')+'</b><small>'+ (item.risk?'Account cần xử lý trong chu kỳ này':'Tạo điểm theo dõi cho Account')+'</small></span></button><button class="customer-control" id="toggleProjectState"><i data-lucide="circle-pause"></i><span><b>'+ (item.state==='active'?'Dừng dự án':'Mở lại dự án')+'</b><small>'+ (item.state==='active'?'Yêu cầu lý do và xác nhận':'Giữ nguyên tiến độ hợp đồng')+'</small></span></button></section></aside></div>';screen.insertAdjacentElement('afterend',detail);detail.querySelector('#backProjects').addEventListener('click',function(){navigate('projects')});detail.querySelector('#editProjectDetail').addEventListener('click',function(){showInfo('Sửa dự án','Prototype ghi nhận quyền sửa cho Account phụ trách. Trường chỉnh sửa: Account, gói dịch vụ, hạn chu kỳ, trạng thái và rủi ro.')});detail.querySelector('#toggleRisk').addEventListener('click',function(){item.risk=!item.risk;renderDetail()});detail.querySelector('#toggleProjectState').addEventListener('click',function(){item.state=item.state==='active'?'pending':'active';renderDetail()});icons();navigate('projectWorkspaceDetail')}
  function openProjectCreate(){var modal=document.createElement('div');modal.className='modal-backdrop show customer-modal';modal.innerHTML='<form class="modal"><div class="modal-top"><h2>Tạo dự án</h2><button class="close" type="button">×</button></div><div class="form"><label class="field">Thương hiệu<input name="customer" required placeholder="Tên thương hiệu"></label><label class="field">Account phụ trách<select name="owner">'+owners.map(function(owner){return '<option>'+owner+'</option>'}).join('')+'</select></label><label class="field">Gói dịch vụ<select name="service"><option>Content duy trì</option><option>Social Content</option><option>Ads duy trì</option></select></label><label class="field">Hạn chu kỳ đầu<input name="due" type="date" required value="2026-10-31"></label><div class="customer-data-rules"><p>Dự án mới luôn ở trạng thái Nháp. Chỉ triển khai khi có Account, hợp đồng hoặc cam kết và chu kỳ đầu tiên.</p></div><div class="form-actions"><button class="secondary" type="button">Hủy</button><button class="primary">Tạo dự án nháp</button></div></div></form>';document.body.appendChild(modal);modal.querySelectorAll('.close,.secondary').forEach(function(button){button.addEventListener('click',function(){modal.remove()})});modal.addEventListener('click',function(event){if(event.target===modal)modal.remove()});modal.addEventListener('submit',function(event){event.preventDefault();var form=event.target,index=records.length+1;records.unshift({id:'project-'+Date.now(),code:'DA-2026-'+String(index).padStart(3,'0'),customer:form.customer.value.trim(),owner:form.owner.value,createdBy:'Tuyền',area:'HCM',service:form.service.value,state:'draft',risk:false,cycle:0,total:1,progress:0,due:form.due.value.split('-').reverse().join('.'),posts:0,shooting:0,tasks:0});state.kpi='all';state.page=1;modal.remove();render()})}
  function openProjectStop(item){var currentAccount=window.CKHubActiveAccount||'Tuyền';if(window.role!=='account'||(item.owner!==currentAccount&&item.createdBy!==currentAccount)){if(window.showToast)window.showToast('Chỉ Account phụ trách hoặc Account tạo dự án được phép dừng.');return}var modal=document.createElement('div');modal.className='modal-backdrop show customer-modal';modal.innerHTML='<form class="modal"><div class="modal-top"><h2>Dừng dự án</h2><button class="close" type="button">×</button></div><div class="form"><div class="customer-data-rules"><b>Phân quyền dừng dự án</b><p>Chỉ Account phụ trách hoặc Account tạo dự án được thực hiện. Tiến độ hợp đồng '+item.cycle+' / '+item.total+' được giữ nguyên.</p></div><label class="field">Lý do dừng<textarea name="reason" required placeholder="Nêu lý do dừng triển khai"></textarea></label><label class="field">Ngày hiệu lực<input name="effectiveDate" type="date" required value="2026-09-22"></label><label class="filter-check"><input name="confirmed" type="checkbox" required> Tôi xác nhận đã kiểm tra ảnh hưởng tới hợp đồng, kế hoạch và công việc.</label><div class="form-actions"><button class="secondary" type="button">Hủy</button><button class="primary">Xác nhận dừng</button></div></div></form>';document.body.appendChild(modal);modal.querySelectorAll('.close,.secondary').forEach(function(button){button.addEventListener('click',function(){modal.remove()})});modal.addEventListener('submit',function(event){event.preventDefault();item.state='stopped';item.risk=false;modal.remove();renderDetail()})}
  function showInfo(title,message){var modal=document.createElement('div');modal.className='modal-backdrop show customer-modal';modal.innerHTML='<div class="modal"><div class="modal-top"><h2>'+title+'</h2><button class="close" type="button">×</button></div><div class="customer-data-rules"><p>'+message+'</p></div><div class="form-actions"><button class="primary" type="button">Đóng</button></div></div>';document.body.appendChild(modal);modal.querySelectorAll('button').forEach(function(button){button.addEventListener('click',function(){modal.remove()})})}
  function normalizeProjectCopy(){var search=screen.querySelector('#projectSearchNew');if(search)search.placeholder='Tìm mã dự án, khách hàng, Account…'}
  new MutationObserver(normalizeProjectCopy).observe(screen,{childList:true,subtree:true});
  screen.addEventListener('click',function(event){if(!event.target.closest('#projectHelp'))return;event.preventDefault();event.stopImmediatePropagation();showInfo('Quy tắc dự án','Mỗi dự án thuộc một khách hàng và một Account phụ trách. Chu kỳ luôn tính theo tháng. Dự án tạm dừng hoặc dừng không tự thay đổi số chu kỳ đã triển khai. Task thiếu Owner hoặc deadline không được bắt đầu.')},true);
  function openProjectCreate(){var choices=base.slice().sort(function(a,b){return a[0].localeCompare(b[0],'vi')}),catalog=window.serviceCatalog||{categories:[],packages:[]},packages=catalog.packages.filter(function(item){return item.status==='Đang áp dụng'}),categories=catalog.categories.filter(function(category){return packages.some(function(item){return item.category===category.id})}),modal=document.createElement('div');if(!packages.length){if(window.showToast)window.showToast('Chưa có gói dịch vụ đang áp dụng.');return}modal.className='modal-backdrop show customer-modal';modal.innerHTML='<form class="modal"><div class="modal-top"><h2>Tạo dự án</h2><button class="close" type="button">×</button></div><div class="form"><label class="field">Khách hàng<input name="customer" type="search" list="projectCustomerOptions" required autocomplete="off" placeholder="Tìm và chọn khách hàng"><datalist id="projectCustomerOptions">'+choices.map(function(item){return '<option value="'+esc(item[0])+'">'}).join('')+'</datalist></label><label class="field">Account phụ trách<select name="owner">'+owners.map(function(owner){return '<option value="'+esc(owner)+'">'+esc(owner)+'</option>'}).join('')+'</select></label><label class="field">Nhóm dịch vụ<select name="serviceCategory">'+categories.map(function(category){return '<option value="'+esc(category.id)+'">'+esc(category.name)+'</option>'}).join('')+'</select></label><label class="field">Gói dịch vụ<select name="servicePackage" required></select></label><label class="field">Hạn chu kỳ đầu<input name="due" type="date" required value="2026-10-31"></label><div class="customer-data-rules"><p>Gói dịch vụ lấy từ danh mục đang áp dụng. Dự án lưu snapshot gói, phạm vi và giá tại thời điểm tạo.</p></div><div class="form-actions"><button class="secondary" type="button">Hủy</button><button class="primary">Tạo dự án nháp</button></div></div></form>';document.body.appendChild(modal);var form=modal.querySelector('form'),selectedCustomer=null;function syncOwner(){selectedCustomer=choices.find(function(item){return item[0].toLocaleLowerCase('vi')===form.customer.value.trim().toLocaleLowerCase('vi')});if(selectedCustomer&&form.dataset.customer!==selectedCustomer[0]){form.owner.value=selectedCustomer[1];form.dataset.customer=selectedCustomer[0]}form.customer.setCustomValidity(selectedCustomer?'':'Chọn khách hàng từ danh sách gợi ý.')}function syncPackages(){var list=packages.filter(function(item){return item.category===form.serviceCategory.value});form.servicePackage.innerHTML=list.map(function(item){return '<option value="'+esc(item.id)+'">'+esc(item.group)+' · '+esc(item.name)+'</option>'}).join('')}syncOwner();syncPackages();form.customer.addEventListener('input',syncOwner);form.serviceCategory.addEventListener('change',syncPackages);modal.querySelectorAll('.close,.secondary').forEach(function(button){button.addEventListener('click',function(){modal.remove()})});modal.addEventListener('click',function(event){if(event.target===modal)modal.remove()});modal.addEventListener('submit',function(event){event.preventDefault();syncOwner();if(!selectedCustomer){form.customer.reportValidity();return}var service=packages.find(function(item){return item.id===form.servicePackage.value}),index=records.length+1;if(!service)return;records.unshift({id:'project-'+Date.now(),code:'DA-2026-'+String(index).padStart(3,'0'),customer:selectedCustomer[0],owner:form.owner.value,createdBy:'Tuyền',area:selectedCustomer[2],service:service.group+' · '+service.name,servicePackageId:service.id,serviceScope:service.scope,servicePrice:service.price,state:'draft',risk:false,cycle:0,total:1,progress:0,due:form.due.value.split('-').reverse().join('.'),posts:0,shooting:0,tasks:0});state.kpi='all';state.page=1;modal.remove();render()})}
  screen.addEventListener('click',function(event){if(!event.target.closest('#createProject'))return;event.preventDefault();event.stopImmediatePropagation();openProjectCreate()},true);
  document.addEventListener('click',function(event){var control=event.target.closest('#toggleProjectState');if(!control||!state.selected)return;event.preventDefault();event.stopImmediatePropagation();if(state.selected.state==='active')openProjectStop(state.selected);else{state.selected.state='active';renderDetail()}},true);
  render();
})();
